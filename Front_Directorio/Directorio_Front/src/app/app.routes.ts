import { Routes } from '@angular/router';
import { DirectoryUsersComponent } from './Directory/directory-users.component';

export const routes: Routes = [
    {
      path: '',
      component: DirectoryUsersComponent,
      title: 'User Directory'
    }
  ];
